[project]
name = "soc_analyst"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "openenv-core>=0.1.0",
    "pydantic>=2.0.0",
    "fastapi>=0.100.0",
    "uvicorn>=0.20.0",
]

[project.scripts]
server = "server.app:main"
